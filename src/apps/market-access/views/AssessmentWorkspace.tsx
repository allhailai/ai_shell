/** Workspace overview shell — package metadata and placeholders come later. */
export function AssessmentWorkspace() {
  return (
    <div
      className="market-access-page"
      role="region"
      aria-labelledby="market-access-workspace-heading"
    >
      <h1 id="market-access-workspace-heading" className="market-access-title">
        Assessment
      </h1>
      <p className="market-access-subtitle">
        Overview for this assessment. Analog, evidence, and knowledge tools are
        not available yet.
      </p>
    </div>
  );
}
