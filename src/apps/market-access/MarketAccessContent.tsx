/**
 * Canvas for Market Access. Phase 1 is a registered placeholder only —
 * list/create/workspace routing lands in later PR 1 phases.
 */
export function MarketAccessContent() {
  return (
    <div
      className="market-access-page"
      role="region"
      aria-labelledby="market-access-heading"
    >
      <h1 id="market-access-heading" className="market-access-title">
        Market Access
      </h1>
      <p className="market-access-subtitle">
        Research pharmaceutical analogs and build evidence-backed assessments.
      </p>
    </div>
  );
}
